/** The head reaches the first line here, then a beat per line after it. */

export const FIRST_LINE = 0.45;

export const LINE_DELAY = 0.16;

/** Where the total sits in the print order, so the count-up starts with it. */

export const TOTAL_LINE = 8;
